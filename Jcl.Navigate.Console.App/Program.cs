using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Runtime.Serialization;
using System.Text;
using System.Threading.Tasks;
using System.Xml.Serialization;

namespace Jcl.Navigate.Console.App
{
    class Program
    {
        static void Main(string[] args)
        {
            //var lineStatusClient = new LineStatusClient();

            //var lineStatuses = lineStatusClient.GetLineStatuses();
            //lineStatusClient.Close();

            //var s = new XmlSerializer(typeof(LineStatusResponse));

            //using (FileStream fs = File.Open("test.xml", FileMode.Open))
            //{
            //    object s2 = s.Deserialize(fs);
            //}
            

        }
    }
}
